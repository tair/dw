/**
 * Copyright 2012 Carnegie Institution for Science. All rights reserved.
 */
// Generated by AndroMDA Poesys/DB cartridge: DO NOT EDIT DIRECTLY.
// Template: AbstractInsert.vsl

package org.tair.db.genomicregion.sql;



import java.sql.PreparedStatement;
import java.sql.SQLException;

import com.poesys.db.dao.insert.IInsertSql;
import com.poesys.db.pk.IPrimaryKey;


/**
 * SQL statement specification for inserting a Polypeptide
 * 
 * @author Robert J. Muller
 */
public class AbstractInsertPolypeptide implements IInsertSql<org.tair.db.genomicregion.IPolypeptide> {
  private static final String SQL =
    "INSERT INTO Polypeptide (aaSequenceId, tairObjectId, genbankDefinition, experimental, description, genbankGi, PirAccession, pl, molecularWeight, ecNumber, tremblId, swissProtId, uniProtId, dateEntered) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)";

  public String getSql(IPrimaryKey key) {
    return SQL;
  }

  public void setParams(PreparedStatement stmt, int index, org.tair.db.genomicregion.IPolypeptide object)
      throws SQLException {
    stmt.setString(index, object.getGenbankDefinition());
    index++;
    stmt.setInt(index, object.getExperimental() ? 1 : 0);
    index++;
    stmt.setString(index, object.getDescription());
    index++;
    stmt.setString(index, object.getGenbankGi());
    index++;
    stmt.setString(index, object.getPirAccession());
    index++;
    stmt.setDouble(index, object.getPl());
    index++;
    stmt.setDouble(index, object.getMolecularWeight());
    index++;
    stmt.setString(index, object.getEcNumber());
    index++;
    stmt.setString(index, object.getTremblId());
    index++;
    stmt.setString(index, object.getSwissProtId());
    index++;
    stmt.setString(index, object.getUniProtId());
    index++;
    stmt.setTimestamp(index, object.getDateEntered());
    index++;
  }
}
