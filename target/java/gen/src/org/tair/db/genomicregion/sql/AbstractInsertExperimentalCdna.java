/**
 * Copyright 2012 Carnegie Institution for Science. All rights reserved.
 */
// Generated by AndroMDA Poesys/DB cartridge: DO NOT EDIT DIRECTLY.
// Template: AbstractInsert.vsl

package org.tair.db.genomicregion.sql;



import java.sql.PreparedStatement;
import java.sql.SQLException;

import com.poesys.db.dao.insert.IInsertSql;
import com.poesys.db.pk.IPrimaryKey;


/**
 * SQL statement specification for inserting a ExperimentalCdna
 * 
 * @author Robert J. Muller
 */
public class AbstractInsertExperimentalCdna implements IInsertSql<org.tair.db.genomicregion.IExperimentalCdna> {
  private static final String SQL =
    "INSERT INTO ExperimentalCdna (seqTairObjectId, tairObjectId, name, sequence, genbankAccession, vectorType, fullLengthCds, length, startPosition, endPosition, dateEntered) VALUES (?,?,?,?,?,?,?,?,?,?,?)";

  public String getSql(IPrimaryKey key) {
    return SQL;
  }

  public void setParams(PreparedStatement stmt, int index, org.tair.db.genomicregion.IExperimentalCdna object)
      throws SQLException {
    stmt.setString(index, object.getName());
    index++;
    stmt.setString(index, object.getSequence());
    index++;
    stmt.setString(index, object.getGenbankAccession());
    index++;
    stmt.setString(index, object.getVectorType());
    index++;
    stmt.setInt(index, object.getFullLengthCds() ? 1 : 0);
    index++;
    stmt.setLong(index, object.getLength());
    index++;
    stmt.setLong(index, object.getStartPosition());
    index++;
    stmt.setLong(index, object.getEndPosition());
    index++;
    stmt.setTimestamp(index, object.getDateEntered());
    index++;
  }
}
