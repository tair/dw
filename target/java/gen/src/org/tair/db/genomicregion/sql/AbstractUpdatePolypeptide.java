/**
 * Copyright 2012 Carnegie Institution for Science. All rights reserved.
 */
// Generated by AndroMDA Poesys/DB cartridge: DO NOT EDIT DIRECTLY.
// Template: AbstractUpdate.vsl

package org.tair.db.genomicregion.sql;


import java.sql.PreparedStatement;
import java.sql.SQLException;

import com.poesys.db.dao.update.IUpdateSql;
import com.poesys.db.pk.IPrimaryKey;


/**
 * SQL statement specification for updating a Polypeptide with read/write properties
 * 
 * @author Robert J. Muller
 */
public class AbstractUpdatePolypeptide implements IUpdateSql<org.tair.db.genomicregion.IPolypeptide> {
  private static final String SQL =
    "UPDATE Polypeptide SET genbankDefinition = ?, experimental = ?, description = ?, genbankGi = ?, PirAccession = ?, pl = ?, molecularWeight = ?, ecNumber = ?, tremblId = ?, swissProtId = ?, uniProtId = ?, dateEntered = ? WHERE ";

  public String getSql(IPrimaryKey key) {
    StringBuilder builder = new StringBuilder(SQL);
    builder.append(key.getSqlWhereExpression(""));
    return builder.toString();
  }

  public int setParams(PreparedStatement stmt, int index, org.tair.db.genomicregion.IPolypeptide object)
      throws SQLException {
    stmt.setString(index, object.getGenbankDefinition());
    index++;
    stmt.setInt(index, object.getExperimental() ? 1 : 0);
    index++;
    stmt.setString(index, object.getDescription());
    index++;
    stmt.setString(index, object.getGenbankGi());
    index++;
    stmt.setString(index, object.getPirAccession());
    index++;
    stmt.setDouble(index, object.getPl());
    index++;
    stmt.setDouble(index, object.getMolecularWeight());
    index++;
    stmt.setString(index, object.getEcNumber());
    index++;
    stmt.setString(index, object.getTremblId());
    index++;
    stmt.setString(index, object.getSwissProtId());
    index++;
    stmt.setString(index, object.getUniProtId());
    index++;
    stmt.setTimestamp(index, object.getDateEntered());
    index++;
    // sets primary key in where clause
    index = object.getPrimaryKey().setParams(stmt, index);
    return index;
  }
}
